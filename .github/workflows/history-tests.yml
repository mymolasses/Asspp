name: History regression tests
on:
  workflow_dispatch:
  push:
    branches: [main, codex/local-sap-tci]
permissions:
  contents: read
jobs:
  metadata:
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4
      - name: Test historical plist date representations
        run: swift test --package-path Vendor/ApplePackage --filter HistoricalDateTests
